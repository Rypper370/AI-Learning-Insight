export default function BadgeModal() {
    return (
        <div>
            // will return a React Portal Modal which returns a list of badges the user has obtained via the leveling system.
        </div>
    )
}